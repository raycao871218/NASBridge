# 通知系统配置
# Telegram配置
TELEGRAM_BOT_TOKEN=your_bot_token_here # 从 @BotFather 获取的机器人token
TELEGRAM_USER_IDS=your_user_id_here # 多个用户ID用逗号分隔
LOG_DIR=/path/to/your/logs # 日志文件存储目录

# SMTP服务器配置（QQ邮箱）
SMTP_SERVER=smtp.qq.com # QQ邮箱SMTP服务器地址
SMTP_PORT=587 # SMTP服务器端口
SMTP_USERNAME=your_qq_number@qq.com # 你的QQ邮箱账号
SMTP_PASSWORD=your_email_auth_code # QQ邮箱授权码，在QQ邮箱设置中获取

# 邮件配置
EMAIL_SENDER=your_qq_number@qq.com # 发件人邮箱，通常与SMTP_USERNAME相同
EMAIL_RECEIVERS=user1@example.com,user2@example.com # 收件人邮箱，多个收件人用逗号分隔

# 域名配置
# 域名列表
DOMAIN_NAME="your.domain.com" # 你的域名

# 证书更新配置
CERT_DNS_TYPE="dns_ali" # DNS验证类型
CERT_DNS_SLEEP=120 # DNS验证等待时间（秒）
CERT_SERVER="letsencrypt" # 证书服务器
CERT_DOCKER_CONTAINER="acme-sh" # acme.sh Docker容器名称

# OPENWRT配置
# OPENWRT 中 NAS IPv6后缀配置
IPV6_SUFFIX="your_ipv6_suffix" # IPv6地址后缀

# 开放端口配置
PORTS_TO_CHECK="80,443,8080" # 需要检查的端口列表，用逗号分隔

# OPENWRT 防火墙配置
FIREWALL_CONFIG_PATH="/etc/config/firewall" # 防火墙配置文件路径
FIREWALL_RESTART_CMD="/etc/init.d/firewall restart" # 防火墙重启命令

# 服务器配置
SERVER_IP="your.server.ip" # 用于NAS同步证书的服务器IP

# NGINX转发脚本配置
NGINX_CONFIG_PATH_AVAILABLE="/etc/nginx/sites-available" # Nginx可用站点配置目录
NGINX_CONFIG_PATH_ENABLED="/etc/nginx/sites-enabled" # Nginx已启用站点配置目录
NGINX_RELOAD_CMD="/etc/init.d/nginx reload" # Nginx重载配置命令
CERT_SAVE_PATH="/root/certs" # 证书保存路径
FIREWALL_TYPE="" # 防火墙类型（根据实际情况修改，可选 ufw 或 iptables）

# IOT 内网配置
OPENWRT_IP="192.168.1.1" # OpenWRT路由器IP地址

# 注意事项：
# 1. TELEGRAM_BOT_TOKEN: 需要在Telegram中与 @BotFather 对话创建机器人并获取token
# 2. TELEGRAM_USER_IDS: 可以通过 @userinfobot 获取你的Telegram用户ID
# 3. CERT_DNS_TYPE: 支持多种DNS验证类型，如dns_ali（阿里云）、dns_cf（Cloudflare）等
# 4. CERT_DNS_SLEEP: DNS验证等待时间，建议不小于120秒
# 5. 所有路径配置请确保对应目录存在且有正确的访问权限
